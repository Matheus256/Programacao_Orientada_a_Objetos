#include "ExpressoHogwarts.h"

ExpressoHogwarts::ExpressoHogwarts &operator<<(const Bruxo &bruxo){
    if(this->Passageiros.size() >= 100){
        std::cout << "Limite de 100 passageiros atingido."
        return *this;
    }

    this->Passageiros.push_back(new )    
}
